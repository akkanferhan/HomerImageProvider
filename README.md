# HomerImageProvider

Modern Swift 6 / iOS 18 image-loading library for the Homer suite of Apple apps.

Async / await throughout, dual-layer caching, automatic downsampling, and PhotoKit integration — built on top of `HomerFoundation` and `HomerUIKit`.

Public API documentation, usage examples, and architecture details land alongside the public surface as it is wired up.
